export default class TodoItem {
  constructor(id, title, description, date, label) {
    this.id = id
    this.title = title
    this.description = description
    this.date = date
    this.label = label
    this.status = status
  }
}
